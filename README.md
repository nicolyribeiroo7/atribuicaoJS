
# 📘 README – Estudo de Operadores de Atribuição em JavaScript

## 📌 Sobre o Projeto

Este conjunto de atividades foi desenvolvido para praticar e compreender os **operadores de atribuição** na linguagem **JavaScript**.

O objetivo principal é entender como valores são atribuídos e modificados dentro de variáveis, utilizando exemplos práticos com interação no navegador.

Cada arquivo HTML demonstra um operador diferente de forma didática, mostrando o passo a passo da execução.

---

## 🎯 Objetivos de Aprendizagem

* Entender o operador de atribuição simples `=`
* Compreender operadores de atribuição combinados:

  * `+=`
  * `*=`
  * `/=`
* Praticar:

  * Entrada de dados com `<input>`
  * Conversão de valores com `Number()`
  * Manipulação do DOM com `getElementById`
  * Exibição de resultados com `innerHTML`

---

## 📂 Arquivos do Projeto

### 1️⃣ atribuicao.html

### ➜ Operador `=`

Demonstra a atribuição simples.

Exemplo:

```js
let x;
x = Number(entrada);
```

📌 O operador `=` atribui um valor a uma variável.

Fluxo da atividade:

1. O usuário digita um valor.
2. O valor é convertido para número.
3. A variável `x` recebe esse valor.
4. O resultado é exibido na tela.

---

### 2️⃣ soma.html

### ➜ Operador `+=`

Demonstra a atribuição com soma.

Exemplo:

```js
x += soma;
```

Equivalente a:

```js
x = x + soma;
```

📌 Esse operador soma um valor à variável e já armazena o novo resultado nela.

---

### 3️⃣ multiplicacao.html

### ➜ Operador `*=`

Demonstra a atribuição com multiplicação.

Exemplo:

```js
x *= mult;
```

Equivalente a:

```js
x = x * mult;
```

📌 Multiplica o valor atual da variável e atualiza automaticamente.

---

### 4️⃣ divisao.html

### ➜ Operador `/=`

Demonstra a atribuição com divisão.

Exemplo:

```js
x /= div;
```

Equivalente a:

```js
x = x / div;
```

📌 Divide o valor da variável e salva o novo resultado.

---

## 🧠 Conceitos Trabalhados

✔ Variáveis com `let`
✔ Conversão de tipos com `Number()`
✔ Operadores matemáticos
✔ Operadores de atribuição
✔ Interação com HTML
✔ Eventos com `onclick`
✔ Manipulação de conteúdo dinâmico

---

## 🖥️ Como Executar

1. Abra qualquer arquivo `.html` no navegador.
2. Digite os valores solicitados.
3. Clique no botão.
4. Observe o passo a passo exibido na tela.

---

## 📈 Evolução do Aprendizado

Essas atividades são fundamentais para:

* Criar cálculos dinâmicos
* Trabalhar com lógica matemática
* Construir sistemas como:

  * Calculadoras
  * Sistemas financeiros
  * Jogos com pontuação
  * Contadores automáticos

Os operadores de atribuição são muito utilizados no desenvolvimento web e fazem parte da base da programação com **JavaScript**.

---

## 🚀 Próximos Passos

Após dominar operadores de atribuição, o próximo passo ideal é estudar:

* Operadores de comparação
* Estruturas condicionais (`if` e `else`)
* Estruturas de repetição (`for`, `while`)
* Funções

---

## 👩‍💻 Conclusão

Essas atividades reforçam o entendimento de como os valores são manipulados dentro das variáveis e mostram, na prática, como os operadores de atribuição funcionam.

Dominar essa base é essencial para avançar no aprendizado de programação.

---

